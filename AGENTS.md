- 任何不明白的问题，先和我确认清楚，再去实现。
- 代码应该合理的抽象，复用，并放在合适的地方。
- 画UI时，应该考虑多语言，参考现有代码。
- 当dart通过bridge调用rust逻辑时，有的返回值是`Future<BridgeSubscription>`。此时应该使用 `convertSubscriptionToStream` 将 `Future<BridgeSubscription>` 转为 Stream。再配合 StreamBuilder 构建UI。
- 检查代码是否可以通过编译，如果不，则修复。
- 如果有单测，则检查单测是否全部可以通过，如果不，则修复。
- 在适当并且必要的地方添加log，具体API参考现有代码。