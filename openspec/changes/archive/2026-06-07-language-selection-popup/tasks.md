## 1. Locale Translations

- [x] 1.1 Add translation strings for language selection titles and options in `rust_wallet/locales/en/main.ftl`.
- [x] 1.2 Add translation strings for language selection titles and options in `rust_wallet/locales/zh/main.ftl`.

## 2. Flutter UI Implementation

- [x] 2.1 Create the language selection bottom sheet dialog with premium design tokens in `lib/src/widgets/common/language_selection_bottom_sheet.dart`.
- [x] 2.2 Add the language selection button to `lib/src/widgets/onboarding_screen.dart` displaying the current active language (resolving `null` to the active system language).
- [x] 2.3 Verify compilation and check that tests run correctly.
